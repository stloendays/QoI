# Headline number registry v0.3

Machine-generated by `analysis/paper_freeze_v03.py`. Use this file as the single source for manuscript headline numbers. Values outside this registry may be used only when their aggregation is explicitly stated.

## scope

- `successful_master_rows` = **6343rows**; n=6343. Denominator: successful reconstructions. Source: `benchmark/master_benchmark_full.csv`.
- `development_materials` = **254materials**; n=254. Denominator: development benchmark. Source: `benchmark/master_benchmark_full.csv`.
- `base_ladder_rows` = **4627rows**; n=4627. Denominator: successful base-ladder reconstructions. Source: `benchmark/master_benchmark_full.csv`.
- `registered_exceptions` = **77rows**; n=77. Denominator: failure registry. Source: `failure_registry.csv`.

## fixed_vs_resolved

- `resolved_greater_than_fixed_fraction` = **0.996974fraction**; n=4627. Denominator: successful base-ladder rows. Source: `benchmark/master_benchmark_full.csv`.
- `pooled_median_resolved_over_fixed` = **52.8041x**; n=4627. Denominator: successful base-ladder rows; paired row ratio. Source: `benchmark/master_benchmark_full.csv`.

## bound_utilization

- `SPERR_median_realized_over_nominal` = **0.999987ratio**; n=1956. Denominator: all successful master rows for codec. Source: `benchmark/master_benchmark_full.csv`.
- `SZ3_median_realized_over_nominal` = **0.999999ratio**; n=1937. Denominator: all successful master rows for codec. Source: `benchmark/master_benchmark_full.csv`.
- `ZFP_median_realized_over_nominal` = **0.157521ratio**; n=2450. Denominator: all successful master rows for codec. Source: `benchmark/master_benchmark_full.csv`.

## matched_realized_linf_complete_case

- `SZ3_over_ZFP_tau1e-3_0.10dex` = **1.81773x** [1.675, 2.014]; n=119. Denominator: A1-eligible, complete-case materials, nominal relative tolerance <=0.01. Source: `analysis_output/complete_case_failure_sensitivity.csv`.
- `SPERR_over_ZFP_tau1e-3_0.10dex` = **2.00234x** [1.791, 2.25]; n=115. Denominator: A1-eligible, complete-case materials, nominal relative tolerance <=0.01. Source: `analysis_output/complete_case_failure_sensitivity.csv`.

## resolvability

- `non_evaluable_fraction_tau_0.0001` = **0.7994fraction**; n=319. Denominator: 319-system stability corpus. Source: `stability/eligibility_summary_A1.csv`.
- `non_evaluable_fraction_tau_0.001` = **0.4138fraction**; n=319. Denominator: 319-system stability corpus. Source: `stability/eligibility_summary_A1.csv`.
- `non_evaluable_fraction_tau_0.01` = **0.0972fraction**; n=319. Denominator: 319-system stability corpus. Source: `stability/eligibility_summary_A1.csv`.

## editorial rules

- Treat same-nominal codec gaps as a mixture of bound utilization and residual structure.
- Use the complete-case matched analysis as the most conservative reviewer-facing sensitivity, while retaining the preregistered A.1-qualified all-successful analysis as the primary protocol result.
- Describe reassignment regression as attenuation/mediation-consistent evidence, not causal mediation.
- Describe the stability floor as protocol-defined/probe-defined.
- Keep the symmetry-equivalent relabelling sensitivity separate from the frozen Protocol A.1 primary numbers.
